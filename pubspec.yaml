name: config_builder
description: Build your app config via code generation from .json files in a type safe way
version: 0.0.2
author: Martin Kamleithner <martin@diagnosia.com>
homepage: https://github.com/knaeckeKami/config_builder

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  source_gen: ^0.9.4+4
  analyzer: ^0.38.2
  build: ^1.1.6

dev_dependencies:

